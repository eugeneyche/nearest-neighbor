#include "nn.h"

int read_bytes(int n_bytes, ifstream & in);
{
}

bool <BundleL> load_set(vector <Bundle> & out_set, ifstream & label_in, ifstream & vtr_in)
{
}

int nn1(Bundle test_set, vector <Bundle> train_set, vector <int> range)
{
}
